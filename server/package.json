{
  "name": "gptlive-sts-demo-server",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "build": "npm run build:client && npm run build:assets",
    "build:client": "esbuild client/app.js --bundle --outfile=public/bundle.js --format=iife",
    "build:assets": "node scripts/generate-connecting-tone.mjs",
    "build:watch": "npm run build:client -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@fastify/formbody": "^8.0.0",
    "@fastify/static": "^10.1.3",
    "@fastify/websocket": "^11.0.0",
    "@twilio/voice-sdk": "^2.18.3",
    "alawmulaw": "^6.0.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.0.0",
    "twilio": "^6.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
